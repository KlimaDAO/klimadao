query getCategories {
  categories {
    id
  }
}

query getCountries {
  countries {
    id
  }
}

query getVintages {
  projects {
    vintage
  }
}

query getPurchasesById($id: Bytes) {
  purchases(first: 1, where: { id: $id }) {
    id
    amount
    listing {
      id
      totalAmountToSell
      leftToSell
      tokenAddress
      active
      deleted
      batches
      batchPrices
      singleUnitPrice
      createdAt
      updatedAt
      seller {
        id
      }
      project {
        id
        key
        projectID
        name
        methodology
        vintage
        projectAddress
        registry
        category {
          id
        }
        country {
          id
        }
        updatedAt
      }
    }
    price
    timeStamp
    user {
      id
    }
  }
}
