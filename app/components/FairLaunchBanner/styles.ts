import { css } from "@emotion/css";

export const fairLaunchBanner = css`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 1rem;
  background-color: var(--darker-gray);
  color: var(--white);
  font-size: 1.4rem;
  font-weight: 600;
  text-align: center;
  gap: 0.6rem;
`;
