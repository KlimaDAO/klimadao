{
  "name": "klimadao",
  "private": true,
  "workspaces": [
    "./site",
    "./app",
    "./lib"
  ],
  "engines": {
    "npm": ">=8.1.2",
    "node": "14.x"
  },
  "scripts": {
    "build-lib": "npm run build -w lib",
    "build-site": "npm run build-lib && npm run build -w site",
    "build-app": "npm run build-lib && npm run build -w app",
    "dev-lib": "npm run dev -w lib",
    "dev-site": "npm run build-lib && npm run dev -w site",
    "dev-app": "npm run build-lib && npm run dev -w app"
  }
}
